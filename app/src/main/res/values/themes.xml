<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (LIGHT THEME). -->
    <style name="Theme.TeretamaApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Text color. -->
        <item name="android:textColor">#111</item>
        <item name="android:fontFamily">@font/lato</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="secondaryTextColor">@android:color/secondary_text_light</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Toolbar -->
        <item name="toolbarElevation">4dp</item>
    </style>
</resources>